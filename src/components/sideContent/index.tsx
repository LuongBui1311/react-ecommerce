import { Flex } from "antd";
import ContentSideBar from "../contentSideBar";

const SideContent = () => {
  return (
    <Flex vertical gap="3">
      <ContentSideBar />
    </Flex>
  );
};

export default SideContent;
